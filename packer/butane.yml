variant: fcos
version: 1.5.0
passwd:
  users:
    - name: root
      password_hash: $y$j9T$q.COzpN65OjTwpmPztXS5/$qiPB5NgWQo.sVqGRqo1/kmPKStfSud5OD7TsQvLOcG7 # vagrant
      ssh_authorized_keys_local:
        - "vagrant.pub.rsa"
        - "vagrant.pub.ed25519"
    - name: core
      password_hash: $y$j9T$q.COzpN65OjTwpmPztXS5/$qiPB5NgWQo.sVqGRqo1/kmPKStfSud5OD7TsQvLOcG7 # vagrant
      ssh_authorized_keys_local:
        - "vagrant.pub.rsa"
        - "vagrant.pub.ed25519"
    - name: vagrant
      password_hash: $y$j9T$q.COzpN65OjTwpmPztXS5/$qiPB5NgWQo.sVqGRqo1/kmPKStfSud5OD7TsQvLOcG7 # vagrant
      ssh_authorized_keys_local:
        - "vagrant.pub.rsa"
        - "vagrant.pub.ed25519"
      home_dir: /home/vagrant
      groups:
        - wheel
        - sudo
storage:
  files:
    - path: /etc/ssh/sshd_config.d/20-enable-passwords.conf
      mode: 0644
      contents:
        inline: |
          # Fedora CoreOS disables SSH password login by default.
          # Enable it.
          # This file must sort before 40-disable-passwords.conf.
          PasswordAuthentication yes
